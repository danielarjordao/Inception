[mysqld]

# Run MariaDB as the mysql user (security best practice)
user            = mysql

# MariaDB listens on this port
port            = 3306

# Listen on all interfaces (enables container-to-container access)
bind_address    = 0.0.0.0

# Data directory for database files
datadir         = /var/lib/mysql
