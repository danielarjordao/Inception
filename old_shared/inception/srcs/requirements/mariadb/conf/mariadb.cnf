[mysqld]

# Run MariaDB as mysql user for security
user            = mysql

# Port for MariaDB connections
port            = 3306

# Listen on all network interfaces (allows connections from other containers)
bind_address    = 0.0.0.0

# Directory where database files are stored
datadir         = /var/lib/mysql
